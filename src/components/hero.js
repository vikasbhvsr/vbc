import React from "react"
import heroStyles from "./hero.module.scss"

const hero = () => {
  return (
    <div className={heroStyles.hero}>
      <h2>Coming Soon!</h2>
    </div>
  )
}

export default hero
